<?xml version="1.0" encoding="ISO-8859-1"?>

  <section>
    <title>Requirements</title>

    <variablelist>
      <varlistentry>
        <term>SWI-Prolog</term>
        <listitem>
          <para>I'm using version 4.0.9. Later versions should also work.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>C++ compiler</term>
        <listitem>
          <para>It is tested with GCC 2.95, but any compliant C++ compiler
            should do. GCC 3.02 does not work, because of bugs.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>Docbook-XML installation</term>
        <listitem>
          <para>The documentation is written using Docbook 4.1.2 (the XML version) and
            Normal Walsh's Docbook XML stylesheets 1.40 for formatting. The
            XSLT processor is xsltproc 1.0.0, but any should do. You need this
            only if you want to change the manual.
          </para>
        </listitem>
      </varlistentry>

    </variablelist>
  </section>
