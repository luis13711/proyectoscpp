#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

void quitar_acentos(string cad);
void quitar_minusculas(string cad);
void palindromo(void);

int main(){ 	
	palindromo();
	return 0; 
	}

void palindromo(void){
	
	int i=0;
	int cert=0;
	int j=0;
	int k=0;
	string cad;
	cout<<"Introuce una frase y te dire si es un palindromo"<<endl;
	getline(cin,cad);

	do{
		if (cad[i]==' '){
			k=i;
			do{
				cad[k]=cad[k+1];
				k++;
			    }while(cad[k]!='\0');
		    }
		i++;
	}while (cad[i]!='\0');
	k = 0;
	j = 0;
	i = i-1;
	
	quitar_acentos(cad);
	quitar_minusculas(cad);
	
	while(cad[i]==cad[j]){
		i--;
		j++;
		if(i==0)
			cert=1;
		}
	if (cert==1)
		cout<<"Si es un palindromo"<<endl;
	else
		cout<<"No es un palindromo"<<endl;
	system("pause");
	}
/*------------------quitar_acentos------------------*/
void quitar_acentos(string cad){
	int i;
	for(i=0;cad[i]!='\0';i++){
		
	toupper(cad[i]);
	switch(cad[i]){
		case '·': cad[i]='A';
			break;
		case 'Ô': cad[i]='E';
			break;
		case 'Þ': cad[i]='I';
			break;
		case 'ã': cad[i]='O';
			break;
		case 'ë': cad[i]='U';
			break;
		case 'µ': cad[i]='A';
			break;
		case '': cad[i]='E';
			break;
		case 'Ö': cad[i]='I';
			break;
		case 'à': cad[i]='U';
			break;
		case 'é': cad[i]='U';
			break;
		}
	}

}
/*-------------------QUITAR MINUSCULAS CON ACENTO---------------*/
void quitar_minusculas(string cad){
	int i;
	for(i=0;cad[i]!='\0';i++)
	switch(cad[i]){
		case '…': cad[i]='A';
			break;
		case 'Š': cad[i]='E';
			break;
		case '': cad[i]='I';
			break;
		case 'o': cad[i]='O';
			break;
		case '—': cad[i]='U';
			break;
		case ' ': cad[i]='A';
			break;
		case '‚': cad[i]='E';
			break;
		case '¡': cad[i]='I';
			break;
		case '¢': cad[i]='O';
			break;
		case '£': cad[i]='U';
			break;
		}
	
	}

